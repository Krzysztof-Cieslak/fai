# Rust build artifacts
/target
**/*.rs.bk

# Fai build outputs (native binaries / object files produced by the compiler)
/out
*.o

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# insta: pending snapshots (accept into *.snap instead of committing)
*.snap.new

# NOTE: Cargo.lock IS committed — Fai ships binaries (compiler + CLI), so the
# lockfile is part of the reproducible build.
