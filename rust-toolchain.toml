# Pinned toolchain for reproducible builds (see docs/MEMORY.md, D19).
# Bumping this is a deliberate, reviewable change.
[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
