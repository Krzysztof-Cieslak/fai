module Status

public classify : { status : Int | _ } -> String
let classify r =
  match r with
  | { status = 0 | _ } -> "ok"
  | { status | _ } -> "error " ++ Int.toString status // irrefutable → exhaustive
